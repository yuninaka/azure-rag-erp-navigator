__pycache__/
*.py[cod]
.venv/
.env
uv.lock.bak
.ruff_cache/
.pytest_cache/
*.egg-info/
.DS_Store
infra/bicep/**/*.bicepparam.local
# az bicep build はコンパイル結果を--stdoutで出す運用のため通常生成されないが、
# 誤って書き出した場合に紛れ込まないように除外しておく。
infra/bicep/**/*.json
# デプロイ時に uv.lock から生成する(scripts/ci_check.shやcd.ymlでは常に再生成するため
# コミットしない。手元で `uv export` を試した際の副産物が紛れ込まないように)。
requirements.txt
